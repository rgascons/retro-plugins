SYSTEM_NAME = nes
SUBMODULE_DIR = nestopia

OUT = $(SUBMODULE_DIR)/libretro/nestopia_libretro.so

$(OUT):
	make -C $(SUBMODULE_DIR)/libretro

submodule-clean:
	make -C $(SUBMODULE_DIR)/libretro clean

-include ../submodule-plugin.mk
