SYSTEM_NAME = pc-engine
SUBMODULE_DIR = beetle-pce-fast-libretro

OUT = $(SUBMODULE_DIR)/mednafen_pce_fast_libretro.so

$(OUT):
	make -C $(SUBMODULE_DIR)

submodule-clean:
	make -C $(SUBMODULE_DIR) clean

-include ../submodule-plugin.mk
