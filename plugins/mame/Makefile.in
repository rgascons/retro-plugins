SYSTEM_NAME = mame
SUBMODULE_DIR = mame

OUT = $(SUBMODULE_DIR)/mame_libretro.so

$(OUT):
	make -C $(SUBMODULE_DIR) -f Makefile.libretro

submodule-clean:
	make -C $(SUBMODULE_DIR) -f Makefile.libretro clean

-include ../submodule-plugin.mk
