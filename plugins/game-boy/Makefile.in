SYSTEM_NAME = game-boy
SUBMODULE_DIR = gambatte-libretro

OUT = $(SUBMODULE_DIR)/libgambatte/gambatte_libretro.so

$(OUT):
	make -C $(SUBMODULE_DIR)/libgambatte -f Makefile.libretro

submodule-clean:
	make -C $(SUBMODULE_DIR)/libgambatte -f Makefile.libretro clean

-include ../submodule-plugin.mk
