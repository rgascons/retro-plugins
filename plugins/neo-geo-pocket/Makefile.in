SYSTEM_NAME = neo-geo-pocket
SUBMODULE_DIR = beetle-ngp-libretro

OUT = $(SUBMODULE_DIR)/mednafen_ngp_libretro.so

$(OUT):
	make -C $(SUBMODULE_DIR)

submodule-clean:
	make -C $(SUBMODULE_DIR) clean

-include ../submodule-plugin.mk
